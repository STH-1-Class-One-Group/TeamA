import { formatDateTime } from '../lib/dates'; import { jsonResponse } from '../lib/http'; import { buildInFilter, encodeFilterValue, supabaseRequest } from '../lib/supabase'; import { readSessionUser } from './auth'; import {   countUnreadNotifications,   createUserNotification,   loadNotificationById,   publishNotificationEvent, } from './notifications';  export function createCommunityRouteService({ loadStaticBaseRows }: { loadStaticBaseRows: (env: any) => Promise<any> }) {   function mapCommunityRoutes(routeRows: any[], routePlaceRows: any[], usersById: Map<any, any>, placesByPositionId: Map<any, any>, likedRouteIds = new Set<any>()) {     const placeRowsByRouteId = new Map();     for (const row of routePlaceRows) {       const routeId = String(row.route_id);       if (!placeRowsByRouteId.has(routeId)) {         placeRowsByRouteId.set(routeId, []);       }       placeRowsByRouteId.get(routeId).push({         stopOrder: row.stop_order,         placeId: placesByPositionId.get(String(row.position_id))?.id ?? String(row.position_id),       });     }      return routeRows.map((row) => {       const placeRows = (placeRowsByRouteId.get(String(row.route_id)) ?? []).sort((left, right) => left.stopOrder - right.stopOrder);       const placeIds = placeRows.map((item) => item.placeId);       return {         id: String(row.route_id),         authorId: row.user_id,         author: usersById.get(row.user_id)?.nickname ?? '이름 없음',         title: row.title,         description: row.description,         mood: row.mood,         likeCount: row.like_count ?? 0,         likedByMe: likedRouteIds.has(String(row.route_id)),         createdAt: formatDateTime(row.created_at),         placeIds,         placeNames: placeIds.map((placeId) => {           for (const place of placesByPositionId.values()) {             if (place.id === placeId) {               return place.name;             }           }           return placeId;         }),         isUserGenerated: row.is_user_generated ?? false,         travelSessionId: row.travel_session_id ? String(row.travel_session_id) : null,       };     });   }    async function requireSessionUser(request: Request, env: any) {     const sessionUser = await readSessionUser(request, env);     if (!sessionUser) {       return { response: jsonResponse(401, { detail: '로그인이 필요해요.' }, env, request) };     }     return { sessionUser };   }    async function readJsonBody(request: Request): Promise<any> {     try {       return await request.json();     } catch {       throw new Error('요청 형식이 올바르지 않아요.');     }   }    async function readRouteRow(env: any, routeId: string) {     const rows = await supabaseRequest(env, `user_route?select=route_id,user_id,like_count&route_id=eq.${encodeFilterValue(routeId)}&limit=1`);     return rows?.[0] ?? null;   }    async function loadCommunityRoutes(env: any, options: any = {}) {     const { sort = 'popular', sessionUserId = null, ownerUserId = null } = options;     const routeFilter = ownerUserId       ? `user_id=eq.${encodeFilterValue(ownerUserId)}&order=created_at.desc`       : `is_public=eq.true&order=${sort === 'popular' ? 'like_count.desc,created_at.desc' : 'created_at.desc'}`;      const routeRows = await supabaseRequest(       env,       `user_route?select=route_id,user_id,travel_session_id,title,description,mood,like_count,created_at,is_public,is_user_generated&${routeFilter}`,     );     const routeIdsFilter = buildInFilter(routeRows.map((row) => row.route_id));     if (!routeIdsFilter) {       return [];     }      const [routePlaceRows, userRouteLikeRows = []] = await Promise.all([       supabaseRequest(env, `user_route_place?select=route_id,position_id,stop_order&route_id=${routeIdsFilter}&order=stop_order.asc`),       sessionUserId         ? supabaseRequest(env, `user_route_like?select=route_id&user_id=eq.${encodeFilterValue(sessionUserId)}&route_id=${routeIdsFilter}`)         : Promise.resolve([]),     ]);      const userIdsFilter = buildInFilter(routeRows.map((row) => row.user_id));     const [{ placeRows }, userRows] = await Promise.all([       loadStaticBaseRows(env),       userIdsFilter ? supabaseRequest(env, `user?select=user_id,nickname&user_id=${userIdsFilter}`) : Promise.resolve([]),     ]);      const neededPositionIds = new Set(routePlaceRows.map((row) => String(row.position_id)));     const placesByPositionId = new Map(       placeRows         .filter((row) => neededPositionIds.has(String(row.position_id)))         .map((row) => [String(row.position_id), { id: row.slug, name: row.name }]),     );     const usersById = new Map(userRows.map((row) => [row.user_id, row]));     const likedRouteIds = new Set(userRouteLikeRows.map((row) => String(row.route_id)));     return mapCommunityRoutes(routeRows, routePlaceRows, usersById, placesByPositionId, likedRouteIds);   }    async function handleCommunityRoutes(request: Request, env: any, url: URL) {     const sessionUser = await readSessionUser(request, env);     const sort = url.searchParams.get('sort') === 'latest' ? 'latest' : 'popular';     const routes = await loadCommunityRoutes(env, { sort, sessionUserId: sessionUser?.id ?? null });     return jsonResponse(200, routes, env, request);   }    async function handleMyRoutes(request: Request, env: any) {     const sessionUser = await readSessionUser(request, env);     if (!sessionUser) {       return jsonResponse(401, { detail: '로그인이 필요해요.' }, env, request);     }     return jsonResponse(200, await loadCommunityRoutes(env, { ownerUserId: sessionUser.id, sessionUserId: sessionUser.id }), env, request);   }    async function handleCreateUserRoute(request: Request, env: any) {     const sessionResult = await requireSessionUser(request, env);     if (sessionResult.response) {       return sessionResult.response;     }      const payload = await readJsonBody(request);     const travelSessionId = String(payload.travelSessionId ?? '').trim();     const title = String(payload.title ?? '').trim();     const description = String(payload.description ?? '').trim();     const mood = String(payload.mood ?? '').trim();     const isPublic = payload.isPublic !== false;      if (!travelSessionId) {       return jsonResponse(400, { detail: '방향을 묶을 여행 세션이 필요해요.' }, env, request);     }     if (!title) {       return jsonResponse(400, { detail: '경로 제목을 적어 주세요.' }, env, request);     }     if (!description) {       return jsonResponse(400, { detail: '한 줄 소개를 적어 주세요.' }, env, request);     }      const sessionRows = await supabaseRequest(       env,       `travel_session?select=travel_session_id,user_id&travel_session_id=eq.${encodeFilterValue(travelSessionId)}&user_id=eq.${encodeFilterValue(sessionResult.sessionUser.id)}&limit=1`,     );     if (!sessionRows?.[0]) {       return jsonResponse(404, { detail: '여행 세션을 찾지 못했어요.' }, env, request);     }      const existingRouteRows = await supabaseRequest(       env,       `user_route?select=route_id&user_id=eq.${encodeFilterValue(sessionResult.sessionUser.id)}&travel_session_id=eq.${encodeFilterValue(travelSessionId)}&limit=1`,     );     if (existingRouteRows?.[0]) {       return jsonResponse(409, { detail: '이미 발행된 여행 코스예요.' }, env, request);     }      const sessionStampRows = await supabaseRequest(       env,       `user_stamp?select=position_id,created_at&travel_session_id=eq.${encodeFilterValue(travelSessionId)}&user_id=eq.${encodeFilterValue(sessionResult.sessionUser.id)}&order=created_at.asc`,     );     const orderedPositionIds = [];     const seenPositionIds = new Set();     for (const stampRow of sessionStampRows ?? []) {       const positionId = String(stampRow.position_id);       if (seenPositionIds.has(positionId)) {         continue;       }       seenPositionIds.add(positionId);       orderedPositionIds.push(positionId);     }     if (orderedPositionIds.length < 2) {       return jsonResponse(400, { detail: '코스에는 최소 두 곳 이상의 스탬프 기록이 필요해요.' }, env, request);     }      const routeRows = await supabaseRequest(env, 'user_route?select=route_id', {       method: 'POST',       body: JSON.stringify({         user_id: sessionResult.sessionUser.id,         travel_session_id: Number(travelSessionId),         title,         description,         mood,         is_public: isPublic,         is_user_generated: true,         like_count: 0,       }),     });     const routeId = routeRows?.[0]?.route_id;      await supabaseRequest(env, 'user_route_place?select=user_route_place_id', {       method: 'POST',       body: JSON.stringify(         orderedPositionIds.map((positionId, index) => ({           route_id: Number(routeId),           position_id: Number(positionId),           stop_order: index + 1,         })),       ),     });      const routes = await loadCommunityRoutes(env, { ownerUserId: sessionResult.sessionUser.id, sessionUserId: sessionResult.sessionUser.id });     const createdRoute = routes.find((route) => route.id === String(routeId)) ?? null;     const createdNotification = await createUserNotification(env, {       userId: sessionResult.sessionUser.id,       actorUserId: sessionResult.sessionUser.id,       type: 'route-published',       title: '새로운 코스가 발행되었습니다.',       body: title,       routeId,       metadata: { travelSessionId },     });     if (createdNotification?.notification_id) {       const notification = await loadNotificationById(env, createdNotification.notification_id);       if (notification) {         await publishNotificationEvent(env, sessionResult.sessionUser.id, 'notification.created', {           notification,           unreadCount: await countUnreadNotifications(env, sessionResult.sessionUser.id),         });       }     }      return jsonResponse(201, createdRoute, env, request);   }    async function handleToggleCommunityRouteLike(request, env, routeId) {     const sessionResult = await requireSessionUser(request, env);     if (sessionResult.response) {       return sessionResult.response;     }      const routeRow = await readRouteRow(env, routeId);     if (!routeRow) {       return jsonResponse(404, { detail: '경로를 찾지 못했어요.' }, env, request);     }     const existingRows = await supabaseRequest(       env,       `user_route_like?select=route_like_id&route_id=eq.${encodeFilterValue(routeId)}&user_id=eq.${encodeFilterValue(sessionResult.sessionUser.id)}&limit=1`,     );     const existing = existingRows?.[0] ?? null;      if (existing) {       await supabaseRequest(env, `user_route_like?route_like_id=eq.${encodeFilterValue(existing.route_like_id)}`, {         method: 'DELETE',         headers: { Prefer: 'return=minimal' },       });     } else {       await supabaseRequest(env, 'user_route_like?select=route_like_id', {         method: 'POST',         body: JSON.stringify({           route_id: Number(routeId),           user_id: sessionResult.sessionUser.id,         }),       });     }      const likeRows = await supabaseRequest(env, `user_route_like?select=route_like_id&route_id=eq.${encodeFilterValue(routeId)}`);     const likeCount = likeRows.length;     await supabaseRequest(env, `user_route?route_id=eq.${encodeFilterValue(routeId)}`, {       method: 'PATCH',       body: JSON.stringify({         like_count: likeCount,         updated_at: new Date().toISOString(),       }),     });      return jsonResponse(200, {       routeId: String(routeId),       likeCount,       likedByMe: !existing,     }, env, request);   }    return {     handleCommunityRoutes,     handleCreateUserRoute,     handleMyRoutes,     handleToggleCommunityRouteLike,     loadCommunityRoutes,   }; }



