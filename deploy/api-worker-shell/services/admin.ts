import { formatDateTime } from '../lib/dates'; import { jsonResponse } from '../lib/http'; import { encodeFilterValue, getSupabaseKey, supabaseRequest } from '../lib/supabase'; import { readSessionUser } from './auth';  async function supabaseCount(env: any, table: string) {   if (!env.APP_SUPABASE_URL) {     throw new Error('APP_SUPABASE_URL is empty.');   }    const apiKey = getSupabaseKey(env);   if (!apiKey) {     throw new Error('Supabase API key is missing.');   }    const response = await fetch(`${env.APP_SUPABASE_URL}/rest/v1/${table}?select=*`, {     method: 'HEAD',     headers: {       apikey: apiKey,       Authorization: `Bearer ${apiKey}`,       Prefer: 'count=exact',     },   });    if (!response.ok) {     const detail = await response.text();     throw new Error(`Supabase count failed (${response.status}): ${detail}`);   }    const contentRange = response.headers.get('content-range') ?? '0-0/0';   const total = Number(contentRange.split('/')[1] ?? '0');   return Number.isFinite(total) ? total : 0; }  export function createAdminService({ normalizePlaceCategory }: { normalizePlaceCategory: (category: any, slug?: string) => string }) {   async function requireAdmin(request: Request, env: any) {     const sessionUser = await readSessionUser(request, env);     if (!sessionUser || !sessionUser.isAdmin) {       return { response: jsonResponse(403, { detail: '관리자만 접근할 수 있어요.' }, env, request) };     }     return { sessionUser };   }    async function buildAdminSummary(env: any) {     const [userCount, placeCount, reviewCount, commentCount, stampCount, placeRows, feedRows] = await Promise.all([       supabaseCount(env, 'user'),       supabaseCount(env, 'map'),       supabaseCount(env, 'feed'),       supabaseCount(env, 'user_comment'),       supabaseCount(env, 'user_stamp'),       supabaseRequest(env, 'map?select=position_id,slug,name,district,category,is_active,is_manual_override,updated_at&order=is_active.desc,name.asc'),       supabaseRequest(env, 'feed?select=position_id'),     ]);      const reviewCountByPosition = new Map();     for (const row of feedRows ?? []) {       const key = String(row.position_id);       reviewCountByPosition.set(key, (reviewCountByPosition.get(key) ?? 0) + 1);     }      return {       userCount,       placeCount,       reviewCount,       commentCount,       stampCount,       sourceReady: true,       places: (placeRows ?? []).map((row) => ({         id: row.slug,         name: row.name,         district: row.district,         category: normalizePlaceCategory(row.category, row.slug),         isActive: Boolean(row.is_active),         isManualOverride: Boolean(row.is_manual_override),         reviewCount: reviewCountByPosition.get(String(row.position_id)) ?? 0,         updatedAt: formatDateTime(row.updated_at),       })),     };   }    async function handleAdminSummary(request: Request, env: any) {     const auth = await requireAdmin(request, env);     if (auth.response) {       return auth.response;     }     return jsonResponse(200, await buildAdminSummary(env), env, request);   }    async function handleAdminPlaceVisibility(request: Request, env: any, placeId: string) {     const auth = await requireAdmin(request, env);     if (auth.response) {       return auth.response;     }      const payload = await request.json().catch(() => null);     const body: Record<string, unknown> = {};     if (typeof payload?.isActive === 'boolean') body.is_active = payload.isActive;     if (typeof payload?.isManualOverride === 'boolean') body.is_manual_override = payload.isManualOverride;     body.updated_at = new Date().toISOString();     const updatedRows = await supabaseRequest(env, `map?slug=eq.${encodeFilterValue(placeId)}`, {       method: 'PATCH',       body: JSON.stringify(body),     });     const updatedRow = Array.isArray(updatedRows) ? updatedRows[0] : null;     if (!updatedRow) {       return jsonResponse(404, { detail: '장소를 찾을 수 없어요.' }, env, request);     }     const reviewRows = await supabaseRequest(env, `feed?select=feed_id&position_id=eq.${encodeFilterValue(updatedRow.position_id)}`);     return jsonResponse(200, {       id: updatedRow.slug,       name: updatedRow.name,       district: updatedRow.district,       category: normalizePlaceCategory(updatedRow.category, updatedRow.slug),       isActive: Boolean(updatedRow.is_active),       isManualOverride: Boolean(updatedRow.is_manual_override),       reviewCount: (reviewRows ?? []).length,       updatedAt: formatDateTime(updatedRow.updated_at),     }, env, request);   }    async function handleAdminImportPublicData(request: Request, env: any) {     const auth = await requireAdmin(request, env);     if (auth.response) {       return auth.response;     }      const sourceRows = await supabaseRequest(       env,       `public_data_source?select=name,last_imported_at&source_key=eq.${encodeFilterValue('jamissue-public-event-feed')}&limit=1`,     );     const source = sourceRows?.[0] ?? null;      return jsonResponse(200, {       importedPlaces: 0,       importedCourses: 0,       importedEvents: 0,       mode: 'scheduled',       detail: '공공 행사는 GitHub Actions 주간 작업으로 처리돼요.',       importedAt: source?.last_imported_at ?? null,     }, env, request);   }    return {     handleAdminImportPublicData,     handleAdminPlaceVisibility,     handleAdminSummary,   }; }


